<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="xyz.ummo.user">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- Required to show current location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Required OpenGL ES 2.0. for Maps V2 -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".delegate.User"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/user_logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:overrideLibrary="com.mongodb.stitch.android">
        <activity android:name=".Feedback"></activity>
        <activity android:name=".Home"></activity>
        <activity
            android:name=".ui.SlideIntro"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.detailedService.DetailedProduct"
            android:label="@string/title_activity_detailed_service"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AllServices"
            android:label="@string/title_activity_all_services"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddCard"
            android:label="@string/title_activity_add_card"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddPaymentMethod"
            android:label="@string/title_activity_add_payment_method"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EditMyProfile"
            android:label="@string/title_activity_edit_my_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DelegationProgress"
            android:label="@string/title_activity_delegation_progress"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DelegationChat"
            android:label="@string/title_activity_delegation_chat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AgentRequest"
            android:label="@string/title_activity_agent_request"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Services"
            android:label="@string/title_activity_services"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.Splash"
            android:label="@string/app_name">
            <intent-filter>
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accept URIs that begin with "http://www.ummo.xyz" -->
                <data
                    android:host="www.ummo.xyz"
                    android:pathPrefix="/user"
                    android:scheme="http" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Location"
            android:label="@string/title_activity_location"></activity>
        <activity android:name=".ui.LoginActivity" />
        <activity
            android:name=".ui.MainScreen"
            android:label="@string/title_activity_confirm_location"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".Register"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="www.ummo.xyz"
                    android:scheme="http" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>